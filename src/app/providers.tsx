'use client';

// 이 파일은 더 이상 사용하지 않습니다. 
// src/components/providers/wallet-provider.tsx로 대체되었습니다.
// 향후 삭제 예정입니다.

import { ReactNode } from 'react';

export default function DeprecatedProviders({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
